\ProvidesPackage{vex_notebook}[2024/01/01 VEX Engineering Notebook Template]

\RequirePackage[utf8]{inputenc}
\RequirePackage{geometry}
\RequirePackage{fancyhdr}
\RequirePackage{graphicx}
\RequirePackage{xcolor}
\RequirePackage{tcolorbox}
\RequirePackage{hyperref}
\RequirePackage{booktabs}
\RequirePackage{tabularx}
\RequirePackage{titlesec}
\RequirePackage{tocloft}
\RequirePackage{ifthen}
\RequirePackage[export]{adjustbox}
\RequirePackage{listings}

% Code Highlighting Setup
\lstset{
    language=C++,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    stringstyle=\color{red},
    commentstyle=\color{green!50!black}\itshape,
    morecomment=[l][\color{magenta}]{\#},
    numbers=left,
    numberstyle=\tiny\color{gray},
    stepnumber=1,
    numbersep=10pt,
    backgroundcolor=\color{white},
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    frame=single,
    rulecolor=\color{black},
    tabsize=4,
    captionpos=b,
    breaklines=true,
    breakatwhitespace=false,
    title=\lstname,
    escapeinside={\%*}{*)}
}

% Page Geometry and Paragraph Styling
\geometry{a4paper, margin=1in, headheight=15pt}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.8em}

% Color mappings for project phases
\definecolor{PhaseDefine}{HTML}{FFB3B3} % Light Red
\definecolor{PhaseGenerate}{HTML}{FFD9B3} % Light Orange
\definecolor{PhaseDevelop}{HTML}{FFFFB3} % Light Yellow
\definecolor{PhaseConstruct}{HTML}{B3FFB3} % Light Green
\definecolor{PhaseEvaluate}{HTML}{B3D9FF} % Light Blue
\definecolor{PhaseDefault}{HTML}{E0E0E0} % Gray

% Graphic path setup: Images can be placed in ./data/assets/
\graphicspath{{./data/assets/}}

% Variables setup
\newcommand{\@teamname}{}
\newcommand{\@teamnumber}{}
\newcommand{\@notebooknumber}{}
\newcommand{\@startdate}{}
\newcommand{\@projectenddate}{}
\newcommand{\@organization}{}
\newcommand{\@teammembers}{}

\newcommand{\teamname}[1]{\renewcommand{\@teamname}{#1}}
\newcommand{\teamnumber}[1]{\renewcommand{\@teamnumber}{#1}}
\newcommand{\notebooknumber}[1]{\renewcommand{\@notebooknumber}{#1}}
\newcommand{\startdate}[1]{\renewcommand{\@startdate}{#1}}
\newcommand{\projectenddate}[1]{\renewcommand{\@projectenddate}{#1}}
\newcommand{\organization}[1]{\renewcommand{\@organization}{#1}}
\newcommand{\teammembers}[1]{\renewcommand{\@teammembers}{#1}}

% Title Page Generation
% Title Page Generation
\newcommand{\maketeamtitle}{
    \thispagestyle{empty}
    \begin{center}
        \vspace*{2cm}
        {\Huge \textbf{\@teamname} \par}
        \vspace{1cm}
        {\huge Team \@teamnumber \par}
        \vspace{2cm}
        {\Large \textbf{Engineering Notebook} \par}
        \vspace{0.5cm}
        {\large Notebook Number: \@notebooknumber \par}
        \vspace{2cm}
        {\large \textbf{Date:} \@startdate{} -- \@projectenddate{} \par}
        \vspace{2cm}
        {\Large \textbf{Organization:} \@organization \par}
        \vspace{2cm}
        {\Large \textbf{Team Members:} \par}
        \vspace{0.5cm}
        {\large \@teammembers \par}
        \vfill
    \end{center}
    \newpage
}

% Fancy header
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\@teamnumber\ - \@teamname}
\fancyhead[R]{Page \thepage}
\fancyfoot[C]{}
\renewcommand{\headrulewidth}{0.4pt}

% Custom TOC format
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}

% Commands to set current entry info
\newcommand{\@entrytitle}{}
\newcommand{\@entrydate}{}
\newcommand{\@entryauthor}{}
\newcommand{\@entryphase}{}
\newcommand{\@entryphasecolor}{PhaseDefault}

% Command to start a new entry
% Usage: \newentry{Title}{Date}{Author}{Phase}
% Phases: Define Problem, Generate Concepts, Develop Solution, Construct and Test, Evaluate Solution
\newcommand{\newentry}[4]{
    \newpage
    \renewcommand{\@entrytitle}{#1}
    \renewcommand{\@entrydate}{#2}
    \renewcommand{\@entryauthor}{#3}
    \renewcommand{\@entryphase}{#4}

    % Determine color based on phase
    \ifthenelse{\equal{#4}{Define Problem}}{\renewcommand{\@entryphasecolor}{PhaseDefine}}{
        \ifthenelse{\equal{#4}{Generate Concepts}}{\renewcommand{\@entryphasecolor}{PhaseGenerate}}{
            \ifthenelse{\equal{#4}{Develop Solution}}{\renewcommand{\@entryphasecolor}{PhaseDevelop}}{
                \ifthenelse{\equal{#4}{Construct and Test}}{\renewcommand{\@entryphasecolor}{PhaseConstruct}}{
                    \ifthenelse{\equal{#4}{Evaluate Solution}}{\renewcommand{\@entryphasecolor}{PhaseEvaluate}}{
                        \renewcommand{\@entryphasecolor}{PhaseDefault}
                    }}}}}

    \phantomsection
    \addcontentsline{toc}{section}{#4 -- #1 (#2)}
    \section*{#1}
    \vspace{-0.5cm}

    \begin{tcolorbox}[colback=\@entryphasecolor, colframe=black, boxrule=1pt, arc=0pt]
        \textbf{Project Title:} \@entrytitle \hfill \textbf{Date:} \@entrydate \\
        \textbf{Author:} \@entryauthor \hfill \textbf{Phase:} \@entryphase
    \end{tcolorbox}
    \vspace{0.5cm}
}

% Legend Page
\newcommand{\phaselegend}{
    \newpage
    \section*{Design Process Phases \& Formatting Guide}

    This notebook is color-coded by the phase of the engineering design process. The table of contents and the header of each entry will reflect these phases.

    \vspace{1cm}

    \begin{tcolorbox}[colback=PhaseDefine, colframe=black, boxrule=1pt, arc=0pt]
        \textbf{Define Problem:} Identifying the core issue, setting SMART goals, outlining constraints and deliverables.
    \end{tcolorbox}

    \begin{tcolorbox}[colback=PhaseGenerate, colframe=black, boxrule=1pt, arc=0pt]
        \textbf{Generate Concepts:} Brainstorming, research, prototyping, and decision matrices to evaluate potential solutions.
    \end{tcolorbox}

    \begin{tcolorbox}[colback=PhaseDevelop, colframe=black, boxrule=1pt, arc=0pt]
        \textbf{Develop Solution:} Creating CAD, detailed sketches, math calculations, graphical models, and pseudocode.
    \end{tcolorbox}

    \begin{tcolorbox}[colback=PhaseConstruct, colframe=black, boxrule=1pt, arc=0pt]
        \textbf{Construct and Test:} Building the robot, writing the code, executing test plans, and gathering qualitative/quantitative data.
    \end{tcolorbox}

    \begin{tcolorbox}[colback=PhaseEvaluate, colframe=black, boxrule=1pt, arc=0pt]
        \textbf{Evaluate Solution:} Reflecting on constraints, event outcomes, and planning future improvements.
    \end{tcolorbox}

    \vspace{1cm}
    \subsection*{General Formatting}
    \begin{itemize}
        \item Keep entries in chronological order.
        \item Ensure all figures have captions and the initials of the author.
        \item Provide detailed explanations for calculations, code logic, and design choices.
    \end{itemize}
    \newpage
}

% Easy image insertion
% Usage: \image{filename}{caption}{initials}
\newcommand{\image}[3]{
    \begin{figure}[htbp]
        \centering
        \includegraphics[max width=\textwidth, max height=0.4\textheight, keepaspectratio]{#1}
        \caption{#2 (Initials: #3)}
    \end{figure}
}

% Standardized Notebook Commands (Pre-prefixed for easy .sty customization)

% \notebookentry{Title}{Date}{Author}{Phase}
\newcommand{\notebookentry}[4]{\newentry{#1}{#2}{#3}{#4}}

% \notebookimage{filename}{caption}{initials}{width_percent}{label}
\newcommand{\notebookimage}[5]{
    \begin{figure}[htbp]
        \centering
        \includegraphics[width=#4, max height=0.4\textheight, keepaspectratio]{#1}
        \caption{#2 (Initials: #3)}
        \label{#5}
    \end{figure}
}

% \notebooktable{columns}{content}{caption}{label}
\newcommand{\notebooktable}[4]{\customtable{#1}{#2}{#3}{#4}}

% \begin{notebookcodeblock}{language}{caption}{label} ... \end{notebookcodeblock}
\newcommand{\notebookcodelabel}[1]{\addvspace{1em}\noindent\textbf{Snippet: #1}\par\nopagebreak\smallskip}
\lstnewenvironment{notebookcodeblock}[3]%
{%
    \notebookcodelabel{#2}%
    \label{#3}%
    \lstset{language=#1, basicstyle=\small\ttfamily, breaklines=true}%
}%
{%
    \addvspace{1em}%
}

% Easy table insertion
% Usage: \customtable{columns (e.g. |c|c|)}{content}{caption}{label}
\newcommand{\customtable}[4]{
    \begin{table}[htbp]
        \centering
        \caption{#3}
        \label{#4}
        \vspace{0.2cm}
        \begin{tabular}{#1}
            \hline
            #2
            \hline
        \end{tabular}
    \end{table}
}
